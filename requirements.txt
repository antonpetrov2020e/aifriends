# Core
python-dotenv==1.0.0

# Telegram Bot
python-telegram-bot==20.7

# AI/LLM Integration
# OpenAI library works with OpenRouter via compatible API
openai>=1.50.0

# Optional: Anthropic direct integration (Phase 2)
# anthropic==0.18.1

# Phase 2 dependencies (optional, uncomment when needed)
# chromadb==0.4.22
# Pillow==10.2.0

# Data Processing (updated for Python 3.13 compatibility)
pydantic>=2.10.0
pydantic-settings>=2.7.0

# Database
sqlalchemy==2.0.25

# HTTP Client (version compatible with python-telegram-bot)
httpx~=0.25.2

# Utils
pytz==2024.1
