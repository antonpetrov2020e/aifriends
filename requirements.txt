# Core
python-dotenv==1.0.0

# Telegram Bot (updated for Python 3.13 compatibility)
python-telegram-bot>=21.0

# AI/LLM Integration
# OpenAI library works with OpenRouter via compatible API
openai>=1.50.0

# Optional: Anthropic direct integration (Phase 2)
# anthropic==0.18.1

# Phase 2 dependencies (RAG Memory + Image Generation)
chromadb>=0.4.22
Pillow>=10.2.0

# Data Processing (updated for Python 3.13 compatibility)
pydantic>=2.10.0
pydantic-settings>=2.7.0

# Database (updated for Python 3.13 compatibility)
sqlalchemy>=2.0.36

# HTTP Client (version compatible with python-telegram-bot 21.x)
httpx>=0.27.0

# Utils
pytz==2024.1

# Database - PostgreSQL support
psycopg[binary]>=3.1.14
alembic>=1.13.0

# Payment - YooKassa
yookassa>=3.2.0
aiohttp>=3.9.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.23.0
pytest-mock>=3.12.0
pytest-cov>=4.1.0

# Linting (dev)
flake8>=6.1.0
black>=23.12.0
